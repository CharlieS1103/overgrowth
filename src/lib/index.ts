
export const app = {
    init: async () => {
        // Initialize the app
        console.log('App initialized');
    }
    // Need to make funcs to communicate with the rust side of the app
    
};